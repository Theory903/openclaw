{
  "id": "ippoc-integration",
  "name": "IPPOC Organism Layer",
  "description": "Integrates IPPOC memory, learning, economy, and swarm intelligence.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ippocNodeUrl": { "type": "string" },
      "ippocApiKey": { "type": "string" },
      "orchestratorMode": { "type": "string", "enum": ["local", "http", "auto"] },
      "orchestratorCli": { "type": "string" },
      "pythonPath": { "type": "string" }
    }
  },
  "uiHints": {
    "ippocNodeUrl": {
      "label": "IPPOC Brain URL",
      "placeholder": "http://localhost:8001",
      "help": "HTTP base URL for the IPPOC Brain (used in HTTP mode or as fallback)."
    },
    "ippocApiKey": {
      "label": "IPPOC API Key",
      "help": "Bearer token for IPPOC Brain authentication.",
      "sensitive": true
    },
    "orchestratorMode": {
      "label": "Orchestrator Mode",
      "help": "Use local Python orchestration, HTTP, or auto fallback."
    },
    "orchestratorCli": {
      "label": "Orchestrator CLI Path",
      "help": "Optional override for the orchestrator_cli.py path."
    },
    "pythonPath": {
      "label": "Python Path",
      "help": "Optional override for the Python executable used in local mode."
    }
  }
}
